
@{
    ViewData["Title"] = "ShowCategoryBlogs";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model List<Blog>

    <div class="col-lg-8 col-md-12 left-box">
        @foreach (var unit in Model)
        {
            <div class="card single_post">
                <div class="body">
                    <div class="img-post">
                        <img class="d-block img-fluid" src="~/pictures/@unit.ImagePath" alt="@unit.Title">
                    </div>
                    <h3>@unit.Title</h3>
                </div>
                <div class="footer">
                    <div class="actions">
                        <a asp-controller="Home" asp-action="ContinueReadingBlog" asp-route-id="@unit.ID" class="btn btn-outline-secondary">Continue Reading</a>
                    </div>
                </div>
            </div>
        }
    </div>


